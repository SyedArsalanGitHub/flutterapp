import 'package:flutter/material.dart';

class HistoryScreen extends StatelessWidget {
  const HistoryScreen(String totaldistance, String totalduration, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
